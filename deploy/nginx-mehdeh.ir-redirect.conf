# mehdeh.ir — redirect only. Path and query are preserved.

server {
    listen 80;
    listen [::]:80;
    server_name mehdeh.ir www.mehdeh.ir;
    return 301 $scheme://mehdi-dehghani.ir$request_uri;
}
