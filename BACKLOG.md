# بک‌لاگ — mehdi-dehghani.ir

موارد باز بعد از نسخهٔ ۱. طرح قفل‌شده در [`PLAN.md`](./PLAN.md).

---

## باز

### B1. لینک تلگرام `@meh_deh`

**وضعیت:** باز  
**نوع:** محتوا / UI

لینک آی‌دی تلگرام به شبکه‌های سایت اضافه شود: [`https://t.me/meh_deh`](https://t.me/meh_deh) (`@meh_deh`).

الان `socials` در `content/profile.ts` فقط GitHub، LinkedIn، X، YouTube و Instagram است. نوع `SocialLink` در `content/types.ts` هم همین شناسه‌ها را می‌پذیرد. فوتر (آیکون) و صفحهٔ تماس (برچسب) از همین آرایه می‌آیند.

**کار:**

- شناسهٔ `telegram` به نوع و آرایهٔ `socials` اضافه شود.
- آیکون تلگرام در `src/components/SocialIcons.tsx`.
- ترتیب نمایش مشخص شود (پیشنهاد: بعد از Instagram، یا بعد از X).
- `rel="me noopener noreferrer"` مثل بقیهٔ لینک‌ها.

---

### B2. دنبالهٔ نویز روی پرترهٔ خانه

**وضعیت:** باز  
**نوع:** تعامل / Canvas  
**فایل:** `src/components/DenoisePortrait.tsx`

الان نویز موضعی فقط دور **موقعیت فعلی** نشان‌گر است (یک دایره با `radius`). با حرکت موس، لکه جابه‌جا می‌شود و مسیر قبلی فوراً پاک می‌شود.

**هدف:** وقتی موس روی پرتره حرکت می‌کند، نویز به‌صورت **دنباله** روی مسیر حرکت بماند و **کم‌کم محو** شود — نه یک نقطهٔ تکی.

**رفتار مورد انتظار:**

- هر نقطهٔ مسیر یک لکهٔ نویز می‌گذارد.
- لکه‌های قدیمی‌تر کم‌کم محو می‌شوند (fade).
- نوک دنباله (موقعیت فعلی) پررنگ‌تر از دم آن است.
- با خروج موس از پرتره، دنباله هم محو شود (نه قطع ناگهانی).
- `prefers-reduced-motion` مثل الان بوم را خالی نگه دارد.

---

## انجام‌شده

_(خالی — نسخهٔ ۱ در PLAN.md)_
